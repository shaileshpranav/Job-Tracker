Drop your base resume here as `resume.pdf` or `resume.docx`.

On first run the app converts it to `resume.md` (the source of truth used for
tailoring). Edit `resume.md` freely; delete it to re-import from the PDF/DOCX.

Optional: `notes.md` — anything else Claude should know about you when writing
(career goals, things you want emphasised, preferred tone).
